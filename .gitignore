# ── Large source video files ───────────────────────────────────────────
# Never commit these — GitHub rejects files >100 MB (your long-form is ~2.1 GB).
# Host finished videos on YouTube/Vimeo and link them via VIDEO_LINKS instead.
*.mp4
*.mov
*.mkv
*.avi
*.webm

# ── Personal working notes (kept private, not part of the site) ──────────
mydetail.md
Aniket_Shaw_Resume.*
style4-remake.html

# ── Tooling / OS ─────────────────────────────────────────────────────────
.vercel/
node_modules/
Thumbs.db
.DS_Store

.vercel

# Loose screenshots / shared images (keep them out of the public repo)
WhatsApp Image*
*.jpeg

# Local mobile render previews (not for the repo)
mobile-preview*

# Design-time files superseded by the live site (kept locally, not published)
draft-style5-editorial.html
mockups/
ee5e876c-176e-4250-821c-f194624495c9.png

# Original poster JPGs — the site now serves the .webp versions
assets/posters/*.jpg
